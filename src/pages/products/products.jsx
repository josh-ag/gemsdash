import { Box, Typography } from "@material-ui/core";

export const ProductsPage = () => {
  return (
    <Box>
      <Typography variant="h5" style={{ textAlign: "center", color: "#444" }}>
        Nothing to display
      </Typography>
    </Box>
  );
};
